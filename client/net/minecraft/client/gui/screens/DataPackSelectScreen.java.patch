--- net/minecraft/client/gui/screens/DataPackSelectScreen.java
+++ net/minecraft/client/gui/screens/DataPackSelectScreen.java
@@ -1,0 +1,22 @@
+package net.minecraft.client.gui.screens;
+
+import java.io.File;
+import java.util.function.Consumer;
+import net.minecraft.client.gui.screens.packs.PackSelectionModel;
+import net.minecraft.client.gui.screens.packs.PackSelectionScreen;
+import net.minecraft.network.chat.TranslatableComponent;
+import net.minecraft.resources.ResourceLocation;
+import net.minecraft.server.packs.repository.Pack;
+import net.minecraft.server.packs.repository.PackRepository;
+
+public class DataPackSelectScreen extends PackSelectionScreen {
+   private static final ResourceLocation DEFAULT_ICON = new ResourceLocation("textures/misc/unknown_pack.png");
+
+   public DataPackSelectScreen(Screen var1, PackRepository<Pack> var2, Consumer<PackRepository<Pack>> var3, File var4) {
+      super(var1, new TranslatableComponent("dataPack.title"), (var2x) -> {
+         return new PackSelectionModel(var2x, (var0, var1) -> {
+            var1.bind(DEFAULT_ICON);
+         }, var2, var3);
+      }, var4);
+   }
+}
