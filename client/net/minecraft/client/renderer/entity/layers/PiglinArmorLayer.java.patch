--- net/minecraft/client/renderer/entity/layers/PiglinArmorLayer.java
+++ net/minecraft/client/renderer/entity/layers/PiglinArmorLayer.java
@@ -1,0 +1,32 @@
+package net.minecraft.client.renderer.entity.layers;
+
+import javax.annotation.Nullable;
+import net.minecraft.client.model.HumanoidModel;
+import net.minecraft.client.renderer.entity.RenderLayerParent;
+import net.minecraft.resources.ResourceLocation;
+import net.minecraft.world.entity.EquipmentSlot;
+import net.minecraft.world.entity.LivingEntity;
+import net.minecraft.world.item.ArmorItem;
+
+public class PiglinArmorLayer<T extends LivingEntity, M extends HumanoidModel<T>, A extends HumanoidModel<T>> extends HumanoidArmorLayer<T, M, A> {
+   private final A headModel;
+
+   public PiglinArmorLayer(RenderLayerParent<T, M> var1, A var2, A var3, A var4) {
+      super(var1, var2, var3);
+      this.headModel = var4;
+   }
+
+   public A getArmorModel(EquipmentSlot var1) {
+      return var1 == EquipmentSlot.HEAD ? this.headModel : super.getArmorModel(var1);
+   }
+
+   protected ResourceLocation getArmorLocation(EquipmentSlot var1, ArmorItem var2, boolean var3, @Nullable String var4) {
+      if (var1 == EquipmentSlot.HEAD) {
+         String var5 = var4 == null ? "" : "_" + var4;
+         String var6 = "textures/models/armor/" + var2.getMaterial().getName() + "_piglin_helmet" + var5 + ".png";
+         return (ResourceLocation)ARMOR_LOCATION_CACHE.computeIfAbsent(var6, ResourceLocation::new);
+      } else {
+         return super.getArmorLocation(var1, var2, var3, var4);
+      }
+   }
+}
